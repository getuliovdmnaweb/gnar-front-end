import { createUseStyles } from "react-jss";

export const useStyles = createUseStyles({
    appBar: {
      display: "flex",
      flexDirection: "row",
      alignItems: "center",
      fontSize: 30,
      fontFamily: "cursive",
      paddingLeft: 50,
      height: 60,
      backgroundColor: "#282C43",
      color: "white",
    },
  });
  